{
  "name": "aigc-live-detector",
  "private": true,
  "version": "0.1.0",
  "description": "Build/test tooling for the AIGC Live Detector browser extension (bonus demo -- see demo/README.md; not a competition deliverable).",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "node --test test/**/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  }
}
